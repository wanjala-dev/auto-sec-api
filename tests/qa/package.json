{
  "name": "autosec-qa",
  "private": true,
  "description": "auto-sec deep identity/admin E2E QA — Playwright Test runner (locators + web-first assertions + kubectl-backed DB verification).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
