{
  "name": "wanjala-gtm-qa",
  "private": true,
  "description": "GTM end-to-end QA — Playwright Test runner (locators + web-first assertions + API verification). Driven by the qa-agent (.claude/agents/qa-agent.md).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
