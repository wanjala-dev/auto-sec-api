-r base.txt

pytest==8.3.3
pytest-cov==5.0.0
pytest-django==4.8.0
# --dist=worksteal added in 3.0; pinning ≥3.6 for tested stability.
# Used by the pre-deploy gate to parallelise the agents + sponsorship + budgeting suites.
pytest-xdist[psutil]==3.6.1

# CI sharding + hang guard (unit-tests.yml matrix)
pytest-split==0.10.0
pytest-timeout==2.3.1
